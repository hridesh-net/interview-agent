version: 1
apps:
  - appID: interview-agent
    appDirPath: .                         # REQUIRED — where your app code lives
    appPort: 8000
    daprHTTPPort: 3500
    daprGRPCPort: 50001
    resourcesPath: "./components"        # your components folder

    env:
      HF_TOKEN: ""
      AWS_REGION: "${AWS_REGION}"
      BEDROCK_MODEL_ID: "${BEDROCK_MODEL_ID}"
      DAPR_HTTP_PORT: "3500"
      SOME_OTHER_CONFIG: "value"

    command: ["uv", "run", "python", "interview_http.py"]